# AUTO-GENERATED by avitoapi.codegen — regenerate, do not hand-edit.
"""Автостратегия — client facade mixin (auto-generated from the Avito OpenAPI spec)."""

from __future__ import annotations

from ..enums.autostrategy import (
    CreateAutostrategyCampaignCampaignType,
    GetAutostrategyBudgetCampaignType,
)
from ..methods.autostrategy import (
    CreateAutostrategyCampaign,
    EditAutostrategyCampaign,
    GetAutostrategyBudget,
    GetAutostrategyCampaignInfo,
    GetAutostrategyCampaigns,
    GetAutostrategyStat,
    StopAutostrategyCampaign,
)
from ..models.autostrategy import (
    Campaigns,
    CreateAutostrategyCampaignResponse,
    EditAutostrategyCampaignResponse,
    GetAutostrategyBudgetResponse,
    GetAutostrategyCampaignInfoResponse,
    GetAutostrategyCampaignsFilter,
    GetAutostrategyCampaignsFilterByUpdateTime,
    GetAutostrategyCampaignsOrderBy,
    GetAutostrategyStatResponse,
    StopAutostrategyCampaignResponse,
)
from ..models.common import TZDatetime
from ._base import FacadeBase


class AutostrategyFacade(FacadeBase):
    """``Client`` mixin — Автостратегия endpoints."""

    async def autostrategy_budget(
        self,
        campaign_type: GetAutostrategyBudgetCampaignType,
        finish_time: TZDatetime | None = None,
        items: list[int] | None = None,
        start_time: TZDatetime | None = None,
    ) -> GetAutostrategyBudgetResponse:
        """Расчет бюджета кампании via ``POST /autostrategy/v1/budget``.

        Args:
            campaign_type: Тип кампании. Возможные типы: `AS` - Автостратегия. Cтратегия расчитана на определенное количество автомобилей, которые находятся в продаже `AP` - Автопилот. Cтратегия расчитана на все автомобили, которые находятся в продаже
            finish_time: Время окончания кампании. **Обязательно только для типа кампании `AS`**
            items: Список индентификаторов объявлений, которые будут продвигаться с помощью данной кампании. **Обязательно только для типа кампании `AS`**
            start_time: Время начала кампании. **Обязательно только для типа кампании `AS`**
        """
        return await self.execute(
            GetAutostrategyBudget(
                campaign_type=campaign_type,
                finish_time=finish_time,
                items=items,
                start_time=start_time,
            )
        )

    async def create_autostrategy_campaign(
        self,
        campaign_type: CreateAutostrategyCampaignCampaignType,
        title: str,
        budget: int | None = None,
        budget_bonus: int | None = None,
        budget_real: int | None = None,
        calc_id: int | None = None,
        description: str | None = None,
        finish_time: TZDatetime | None = None,
        items: list[int] | None = None,
        start_time: TZDatetime | None = None,
    ) -> CreateAutostrategyCampaignResponse:
        """Создание новой кампании via ``POST /autostrategy/v1/campaign/create``.

        Args:
            budget: Выделенный бюджет на кампанию в рублях. **Обязательно только для типа кампании `AS`**
            budget_bonus: Бонусная часть бюджета, в рублях, которая спишется засчет бонусов. **Может быть только для типа кампании `AP`**
            budget_real: Часть бюджета на кампанию в рублях. **Обязательно только для типа кампании `AP`**
            calc_id: Идентфикатор расчета бюджета из ответа ручки `/autostrategy/v1/budget`
            campaign_type: Тип кампании. Возможные типы: `AS` - Автостратегия. Cтратегия расчитана на определенное количество автомобилей, которые находятся в продаже `AP` - Автопилот. Cтратегия расчитана на все автомобили, которые находятся в продаже
            description: Описание кампании
            finish_time: Время окончания кампании. **Обязательно только для типа кампании `AS`**
            items: Список индентификаторов объявлений, которые будут продвигаться с помощью данной кампании. **Обязательно только для типа кампании `AS`**
            start_time: Время начала кампании. **Обязательно только для типа кампании `AS`**
            title: Название кампании
        """
        return await self.execute(
            CreateAutostrategyCampaign(
                budget=budget,
                budget_bonus=budget_bonus,
                budget_real=budget_real,
                calc_id=calc_id,
                campaign_type=campaign_type,
                description=description,
                finish_time=finish_time,
                items=items,
                start_time=start_time,
                title=title,
            )
        )

    async def edit_autostrategy_campaign(
        self,
        campaign_id: int,
        version: int,
        budget: int | None = None,
        calc_id: int | None = None,
        description: str | None = None,
        finish_time: TZDatetime | None = None,
        items: list[int] | None = None,
        start_time: TZDatetime | None = None,
        title: str | None = None,
    ) -> EditAutostrategyCampaignResponse:
        """Редактирование кампании via ``POST /autostrategy/v1/campaign/edit``.

        Args:
            budget: Новый бюджет кампании в рублях
            calc_id: Идентификатор расчета бюджета из ответа ручки `/autostrategy/v1/budget`
            campaign_id: Идентификатор активной кампании
            description: Новое описание кампании
            finish_time: Новое время окончания кампании
            items: Новый список индентификаторов объявлений, которые будут продвигаться с помощью данной кампании
            start_time: Новое время начала кампании
            title: Новое название кампании
            version: Текущая версия кампании
        """
        return await self.execute(
            EditAutostrategyCampaign(
                budget=budget,
                calc_id=calc_id,
                campaign_id=campaign_id,
                description=description,
                finish_time=finish_time,
                items=items,
                start_time=start_time,
                title=title,
                version=version,
            )
        )

    async def autostrategy_campaign_info(
        self, campaign_id: int
    ) -> GetAutostrategyCampaignInfoResponse:
        """Получение полной информации о кампании via ``POST /autostrategy/v1/campaign/info``.

        Args:
            campaign_id: Идентификатор активной кампании
        """
        return await self.execute(GetAutostrategyCampaignInfo(campaign_id=campaign_id))

    async def stop_autostrategy_campaign(
        self, campaign_id: int, version: int
    ) -> StopAutostrategyCampaignResponse:
        """Остановка кампании via ``POST /autostrategy/v1/campaign/stop``.

        Args:
            campaign_id: Идентификатор активной кампании
            version: Текущая версия кампании
        """
        return await self.execute(
            StopAutostrategyCampaign(campaign_id=campaign_id, version=version)
        )

    async def autostrategy_campaigns(
        self,
        limit: int,
        by_update_time: GetAutostrategyCampaignsFilterByUpdateTime | None = None,
        offset: int | None = None,
        order_by: list[GetAutostrategyCampaignsOrderBy] | None = None,
        status_id: list[int] | None = None,
    ) -> Campaigns:
        """Получение списка кампаний via ``POST /autostrategy/v1/campaigns``.

        Args:
            by_update_time: Фильтрация кампаний по дате обновления
            limit: Ограничение на выборку
            offset: Смещение выборки, по умолчанию равно 0
            order_by: Поля, по которым будет произведена сортировка, и тип сортировки
            status_id: Статусы кампании для выборки. Возможные статусы: `0` - Черновик, дата старта сегодня или раньше, денег на балансе нет `1` - Активная кампания `2` - Кампания приостановлена `3` - Кампания удалена `4` - Кампания остановлена `5` - Кампания запланирована, дата старта завтра или позже, на балансе есть деньги По умолчанию в выборку попадают кампании со статусами 0, 1, 2, 4, 5.
        """
        return await self.execute(
            GetAutostrategyCampaigns(
                filter=GetAutostrategyCampaignsFilter(by_update_time=by_update_time),
                limit=limit,
                offset=offset,
                order_by=order_by,
                status_id=status_id,
            )
        )

    async def autostrategy_stat(self, campaign_id: int) -> GetAutostrategyStatResponse:
        """Получение статистики по кампании via ``POST /autostrategy/v1/stat``.

        Args:
            campaign_id: Идентификатор запущенной или уже прошедшей кампании
        """
        return await self.execute(GetAutostrategyStat(campaign_id=campaign_id))
